# Core numerical & image libraries
numpy>=1.24.0
opencv-contrib-python>=4.7.0  # includes ArUco (cv2.aruco)
av>=8.0.3
Pillow>=8.4.0

# ML / YOLO
ultralytics>=8.0.0
# `torch` is required for many ultralytics workflows —
# install the correct wheel for your platform (CPU/CUDA) manually if needed
torch>=2.0.0

# Data + utilities
PyYAML>=6.0
pandas>=1.5.0
matplotlib>=3.6.0
plotly>=5.0.0
tqdm>=4.65.0

# Drone / control
djitellopy>=3.10
simple-pid>=0.1.3
pygame>=2.1.0

# ML tools
scikit-learn>=1.2.0